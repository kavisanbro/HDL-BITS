module top_module (
    input  [3:0] in,
    output reg [2:0] pos
);

    always @(*) begin
        casez (in)
            4'b???1: pos = 3'd0;   // check LSB first
            4'b??10: pos = 3'd1;
            4'b?100: pos = 3'd2;
            4'b1000: pos = 3'd3;
            default: pos = 3'd0;   // if no bits are set → return 0
        endcase
    end

endmodule

